[tool.poetry]
name = "recommendation"
version = "0.1.0"
description = "Recommendation system module"

[tool.poetry.dependencies]
python = "^3.11"
numpy = "*"
faiss_cpu = "*"
torch = "*"
transformers = "*"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

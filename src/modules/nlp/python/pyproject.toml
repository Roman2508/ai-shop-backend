[tool.poetry]
name = "nlp"
version = "0.1.0"
description = "NLP module"
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"
spacy = "*"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
